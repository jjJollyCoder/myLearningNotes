
# 账单分类项目 Debug 进度报告

日期：2025-07-07

---

## ✅ 今日已完成内容

* ✅ 项目目录结构搭建完成，包含 `data/raw`、`data/processed`、`src/` 等文件夹
* ✅ 实现基础的账单预处理框架，能批量遍历 `data/raw` 目录下的账单文件
* ✅ 初步完成支付宝、微信、信用卡账单的文件读取函数 `read_bill_file()`
* ✅ 定位支付宝、微信账单 CSV 文件存在前几行非数据内容，确定跳过行数（支付宝24行，微信16行）
* ✅ 实现跳过指定行数读取 CSV 文件，解决部分乱码和格式混乱问题
* ✅ 解决部分路径找不到的问题，明确运行目录与路径的关系
* ✅ 对微信账单单文件进行独立调试，验证部分参数（encoding、skiprows、engine）有效性

---

## 🔄 正在调试中 / 待确认

* ❌ 支付宝账单部分文件读取仍报错：`Error tokenizing data`，怀疑字段分隔符或格式不规范
* ❌ 进一步排查 CSV 分隔符（逗号、制表符等）及字段引号包裹情况
* ❌ 设计更鲁棒的账单读取函数，自动识别分隔符和跳行数
* ❌ 支付宝账单编码格式确认，处理乱码问题
* ❌ 统一微信多文件批量处理逻辑，支持批量文件循环读取和转换
* ❌ 确认项目运行路径规范，避免因路径问题导致文件找不到
* ❌ 编写测试用例及脚本，覆盖不同账单格式读取
* ❌ 设计并实现账单分类模型的训练数据准备模块

---

## 📅 下一步计划

* 🛠 继续分析支付宝账单文件格式，尝试不同参数解决读取异常
* 🛠 用文本编辑器手动核查文件结构和字段，优化跳过行数和分隔符设置
* 🛠 优化 `read_bill_file()` 函数，支持更多账单格式及异常容错
* 🛠 完善日志和错误捕获，方便定位和跟踪问题
* 🛠 开始设计账单数据标签体系，准备训练数据结构

---
# 🧾 账单分类研究记录（阶段性总结）

记录时间：2025年7月3日 - 7月11日  
研究目标：实现多平台账单的自动分类、结构化展示和用途分析，支持个人和家庭财务管理。

---

## 📆 2025-07-03（初步构思）

### ✅ 今日进展：
- 确立账单分类的目标：从原始平台账单提取“消费内容”、“消费方”、“时间”、“金额”；
- 明确核心字段：商品说明、交易对方、交易备注为分类关键字段；
- 确定以“用途”为主轴设计分类系统，覆盖日常消费、家庭支出、报销等；

### ❗️ 遇到的问题：
- 平台字段命名不一致（如支付宝 vs 微信）；
- 无法从商品描述中直观区分用途；
- 同一商户可能多种用途（如全家既买饮料也买火车票）；

### 💡 尝试与方案：
- 梳理所有平台字段名，对齐为统一命名结构；
- 先用人工对账单打上分类标签，形成初始样本；
- 尝试用“关键词 + 正则”方式进行第一批自动分类。

---

## 📆 2025-07-04（自动分类初探）

### ✅ 今日进展：
- 开始编写 `classify_text()` 函数，用关键词和正则匹配方式归类；
- 初步设计分类大类（如：日常消费、出行、育儿、医疗）和细分类别；
- 对账单中部分典型交易做静态规则匹配测试。

### ❗️ 遇到的问题：
- 部分关键词会跨类别（如“盒马”既可算日常消费也可能是宴请）；
- 模糊匹配中出现误判：如“教育培训”被误分到“工作报销”；
- 所有分类信息嵌入代码，维护成本高。

### 💡 尝试与方案：
- 设计关键词优先级，建立 `优先规则 → 次级规则`；
- 把规则写入外部 JSON 文件，代码中只做解析；
- 每条匹配记录都打印 debug 信息，方便追踪分类结果。

---

## 📆 2025-07-05 ~ 07（分类结构与规则搭建）

### ✅ 今日进展：
- 梳理初步的大类/细类结构，形成层级分类体系；
- 大类示例：消费类（吃喝/衣物/交通）、家庭类（育儿/租房/水电煤）、个人成长（教育/健身/课程）、工作报销类；
- 建立了数十条初步分类规则（“便利蜂”→吃喝，“儿童医院”→育儿医疗）；

### ❗️ 遇到的问题：
- 有些交易描述无法单一对应分类（如“京东”下有几乎所有种类）；
- 商品说明过于简略，难以自动归类；
- 开始考虑如何可视化管理规则集。

### 💡 尝试与方案：
- 引入“平台+关键词组合规则”，提升精度；
- 添加 `source` 字段（如 alipay/wechat/creditcard）区分规则作用范围；
- 计划未来构建规则管理页面或配置表格用于动态维护。

---

## 📆 2025-07-08（多平台适配与结构优化）

### ✅ 今日进展：
- 开始针对微信账单单独处理分类逻辑；
- 发现微信账单需通过备注区分实际消费内容（如朋友转账 vs 用于拼车）；
- 开始整理规则为 JSON 文件，尝试与实际交易内容进行对比验证。

### ❗️ 遇到的问题：
- 同样是“转账”类别，备注内容千差万别；
- 微信分类依赖备注填写质量，容易分类模糊；
- “退款”、“转入”等特殊情况影响总消费计算；

### 💡 尝试与方案：
- 建立 `微信专属规则表`，以“备注关键词 + 转账方向”为核心；
- 对备注字段进行分词、停用词清洗后再匹配；
- 为每一笔交易增加“特殊标记”字段（如：退款标志）便于后续汇总排除。

---

## 📆 2025-07-09（分类标准与可视化初探）

### ✅ 今日进展：
- 明确分类需要以“主用途”为核心：不做多分类，而是以单一主标签为主；
- 研究支付宝日历账单视图、微信账单整体趋势报告，参考其分类维度；
- 探索如何用图表展示年度支出占比、标签趋势等。

### ❗️ 遇到的问题：
- 一些账单无法归类（如：没有备注、商户名也很通用）；
- 多个标签下难以决定主归属；
- 报表生成逻辑尚未设计。

### 💡 尝试与方案：
- 引入“用途说明”字段，允许用户选择补充用途；
- 使用 Excel 辅助建模，先模拟分类和报表生成；
- 规划后续使用 `matplotlib` 或 `plotly` 实现 Python 自动图表分析。

---

## 📆 2025-07-10（统一平台分类机制设计）

### ✅ 今日进展：
- 构建一个统一入口函数 `run_classification()`；
- 平台识别机制上线（通过“原始平台”字段判断使用哪套规则）；
- 研究是否以“配置驱动分类 + 多维标签”方式重构分类系统。

### ❗️ 遇到的问题：
- 有的分类过于细碎，导致汇总难度上升；
- 有成员提到“其实我也分不清自己到底花了多少钱在吃喝上”；
- 标签之间互斥逻辑尚未建立。

### 💡 尝试与方案：
- 引入“标签归一规则”：如“饮料”、“水果”→统一归类为“吃喝”；
- 优化输出格式，保留交易原始信息 + 自动分类 + 人工用途字段；
- 计划后续构建“标签汇总表”与“标签合并映射表”。

---

## 📆 2025-07-11（截图分析与字段思考）

### ✅ 今日进展：
- 上传账单截图，思考字段应由系统填还是人工填；
- 样例包括时间、金额、商品说明、交易对方、大类、标签、说明等字段；
- 明确了字段结构对“可追踪性”和“用途透明度”的关键作用。

### ❗️ 遇到的问题：
- “大类”、“标签”、“说明”能否完全自动化判断存在疑问；
- 手动标记分类效率较低，且易产生主观误差；
- 如果后期每月都分析，如何提高复用率？

### 💡 尝试与方案：
- 第一阶段：人工标记 + 自动匹配（使用 Excel VLOOKUP 或 Python 脚本）；
- 第二阶段：建立关键词 → 分类的标准字典库，做成可共享数据资产；
- 第三阶段：探索 GPT 接入方式，对备注/交易描述进行语义理解辅助分类。

---


📎 后续建议：

建议将规则字典与样本记录同步管理（可用 Excel 或 SQLite）；
尝试部署一个本地可视化小工具，让你家人也能参与标注或回顾账单；
自动脚本规划：
扫描账单目录；
执行关键词匹配分类；
导出分类结果为 CSV；
输出图表（如支出雷达图、饼图、趋势图）；
若你后续决定使用 Obsidian，我也可以帮你生成一套每日账单研究的 Daily Template 模板。